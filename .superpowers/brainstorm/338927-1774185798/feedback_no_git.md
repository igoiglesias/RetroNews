---
name: feedback_browser_self
description: User wants me to use browser-devtools to view mockups myself instead of asking them to open URLs
type: feedback
---

User prefers that I use the browser-devtools plugin to view and interact with mockups myself, rather than asking them to open browser URLs manually.

**Why:** More efficient workflow — user doesn't need to context-switch to a browser.
**How to apply:** When doing visual brainstorming, use browser-devtools tools (take_screenshot, etc.) to view the mockups and present screenshots to the user directly.
